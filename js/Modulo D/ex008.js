var velocidade = 30
if (velocidade > 0 && velocidade < 40) {
    console.log(`Sua velocidade está ideal para a via frequentada, continue assim`);
} else {
    console.log(`Sua velocidade de ${velocidade} exede o permitido você recebeu uma multa`);
}